import React from "react";
import AdminLoginFlow from "@/components/admin/AdminLoginFlow";

export const metadata = {
  title: "Admin Login · Giveaway",
};

export default function AdminLoginPage() {
  return <AdminLoginFlow />;
}
